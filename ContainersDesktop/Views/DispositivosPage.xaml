<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ContainersDesktop.Views.DispositivosPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d"
    >
    <Border>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <CommandBar Background="Transparent"
                        DefaultLabelPosition="Right"
                        Grid.Column="1">
                <AppBarButton Command="{x:Bind NuevoCommand, Mode=OneWay}"
                              Icon="Add"
                              Label="Agregar" />
                <AppBarButton Command="{x:Bind BorrarCommand}"
                              CommandParameter="{x:Bind ViewModel.SelectedDispositivo, Mode=OneWay}"
                              IsEnabled="{x:Bind ViewModel.HasCurrent, Mode=OneWay}" 
                              Icon="Delete"
                              Label="Borrar"/>
                <AppBarButton Command="{x:Bind SincronizarCommand}"
                              Icon="Sync"
                              Label="Sincronizar"/>
            </CommandBar>
            
            <controls:DataGrid
                Name="DispositivosGrid"
                Grid.Row="1"
                AutoGenerateColumns="False"
                GridLinesVisibility="Horizontal"
                SelectedItem="{Binding SelectedDispositivo}"
                ItemsSource="{x:Bind ViewModel.Source}"
                RowEditEnding="DispositivosGrid_RowEditEnding"
                SelectionMode="Single"
                SelectionChanged="DispositivosGrid_SelectionChanged"
                AlternatingRowBackground="DarkSlateGray">
                <controls:DataGrid.Resources>
                    <SolidColorBrush x:Key="DataGridColumnHeaderBackgroundColor" Color="Transparent" />
                </controls:DataGrid.Resources>
                <controls:DataGrid.Columns>
                    <controls:DataGridTextColumn Binding="{Binding DISPOSITIVOS_ID_REG}" Header="ID" Visibility="Collapsed" />
                    <controls:DataGridTextColumn Binding="{Binding DISPOSITIVOS_DESCRIP}" Header="Descripción" />
                    <controls:DataGridTextColumn Binding="{Binding DISPOSITIVOS_CONTAINER}" Header="Cloud Container" />
                </controls:DataGrid.Columns>
            </controls:DataGrid>

            <ContentDialog x:Name="AgregarDialog"
                            PrimaryButtonText="Aceptar"
                            CloseButtonText="Cancelar"
                            >
                <RelativePanel HorizontalAlignment="Stretch">
                    <TextBox x:Name="Dispositivo"
                        Header="Dispositivo"
                        Text="{Binding DISPOSITIVOS_DESCRIP, Mode=TwoWay}"
                             MaxWidth="500"/>
                    <TextBox x:Name="CloudContainer"
                        Header="Cloud Container"
                        Text="{Binding DISPOSITIVOS_CONTAINER, Mode=TwoWay}"
                        RelativePanel.RightOf="Dispositivo"
                        RelativePanel.AlignRightWithPanel="True"
                        Margin="0,0,0,10" />
                </RelativePanel>
            </ContentDialog>
        </Grid>
    </Border>
</Page>
